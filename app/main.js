define(function (require) {
    "use strict";

    require(['bootstrap']);
//    var Backbone = require("backbone"),
//        app = require('app');

//    new Router({ controller: Controller });

});